# StockPilot V0.1 验收记录

验收日期：2026-09-26。状态：**部分通过，尚未达到完整 V0.1 验收**。

| 项目 | 结果 | 证据 |
|---|---|---|
| Dashboard / 搜索 / 详情 / K 线 / 自选股 | 部分通过 | 隔离数据库的真实接口测试中，三大指数 200、股票搜索 200、宁德时代报价 200，自选股增查删为 201/200/204；日 K 与周 K 均为 503。随后行情源又间歇断开，浏览器中指数与报价显示错误重试状态。浏览器搜索可跳转详情，自选股可添加，日/周 K 可切换并显示错误。 |
| Agent 核心任务 | 部分通过 | 真实 DeepSeek 对指数问题调用 `get_market_indices`，成功返回回答；行情源断开时返回 503，没有伪造行情回答。依赖 K 线的真实任务尚未通过。 |
| Trace | 通过 | 上述成功调用记录了 `get_market_indices/success`；浏览器中数据源失败调用记录了 `get_market_indices/error`，会话 Trace 可查询。 |
| Evaluation | 通过 | 本轮真实模型冒烟运行 2 条 Case，三项指标均为 2/2；完整 24 条运行结果见 README（2026-09-26，三项均为 22/24）。评测行情是固定数据，不代表真实行情源验收。 |
| 后端测试 | 通过 | `pytest -q`：35 passed。 |
| 前端检查 | 通过 | `npm run lint`、`npx tsc --noEmit`、`npm run build` 均通过。 |
| 文档与代码 | 通过 | README 提供 Quick Start、环境变量、截图、架构图、免责声明及真实 Eval 结果；示例截图已标明固定数据。 |
| Git 历史与密钥 | 通过 | 验收开始时 `main` 与 `origin/main` 同步、工作区干净；历史中 245 个文本 blob 按常见 Key 格式扫描未命中。被跟踪的环境文件只有两个不含密钥的 `.env.example` 模板，真实 `backend/.env` 被 Git 忽略。 |

## 未通过项：东方财富 K 线

`push2his.eastmoney.com/api/qt/stock/kline/get` 的日 K 和周 K 请求在连接阶段出现 `RemoteProtocolError`，API 映射为 503。同源候选 `push2delay`、`push2` 的 K 线路径也断开；尚无经验证可用的同源替代节点。指数、报价曾成功返回，之后也出现相同的间歇性连接错误。因此当前不能将 Dashboard 全链路或 Agent 核心任务标为完全通过。固定响应单测和固定行情页面截图仅证明解析、UI 与错误处理能力。

待行情源可用时，重新验证真实指数、搜索、个股报价、日 K、周 K、自选股批量报价，以及 Agent 的个股 K 线和两股比较任务。所有项目通过后再完成 `TODO.md` 第 10 阶段。
